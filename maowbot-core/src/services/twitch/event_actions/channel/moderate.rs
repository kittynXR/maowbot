use crate::Error;

/// Stubs for channel.moderate(1,2), channel.moderator.add, channel.moderator.remove, etc.
pub async fn handle_moderation_action() -> Result<(), Error> {
    // channel.moderate
    Ok(())
}

pub async fn handle_moderator_add() -> Result<(), Error> {
    // channel.moderator.add
    Ok(())
}

pub async fn handle_moderator_remove() -> Result<(), Error> {
    // channel.moderator.remove
    Ok(())
}
